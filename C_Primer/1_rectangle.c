#include <stdio.h>
int main()
{
    int length, width, result;
    printf("Enter length: ");
    printf("Enter width: ");
    result = length * width;
    printf("Area = %d\n", result);
    return 0;
}